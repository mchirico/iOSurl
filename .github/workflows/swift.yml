name: Swift

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Instruments
      run: xcrun instruments -s devices
    - name: Build
      run: xcodebuild clean build -project iOSurl.xcodeproj -scheme iOSurl CODE_SIGNING_REQUIRED=NO -destination "platform=iOS Simulator,name=iPhone 11 Pro" -quiet
